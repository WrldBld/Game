# Procfile for Overmind - Development mode
# Run with: overmind start -f Procfile.dev
# Or simply: task dev

# Backend Engine server
backend: RUST_LOG=info,wrldbldr_engine=debug cargo run -p wrldbldr-engine-runner --bin wrldbldr-engine

# Frontend Player (web/WASM with hot reload)
# Ensure npm deps exist so tailwindcss is available.
frontend: cd crates/player-ui && ([ -d node_modules ] || npm ci) && RUST_LOG=info,wrldbldr_player=debug npm run build:css && cd ../player-runner && dx serve --web --open=false

# Tailwind CSS watcher (optional - uncomment if needed)
# css: cd crates/player-ui && npm run watch:css
